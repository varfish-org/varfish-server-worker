namespace VarFishServerWorker;

// Enum for storing the SV type.
enum SvType : byte {
  Del,
  Dup,
  Inv,
  Ins,
  Bnd,
  Cnv,
}

// Background database record.
struct BgDbRecord {
  chrom_no: ubyte;
  chrom_no2: ubyte;
  sv_type: SvType;
  start: uint;
  end: uint;
  count: uint;
}

